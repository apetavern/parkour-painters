@namespace ParkourPainters.UI

@if (MapVoteState.Instance is null)
{
    return;
}

<root>
    @* TODO: Replace with data populated inside of MapVoteState. *@
    <div class="maps">
        <MapIcon Ident="apetavern.pp_streets" />
        <MapIcon Ident="apetavern.pp_rooftops" />
    </div>
</root>

@code {
    protected override int BuildHash()
    {
        return 0;
    }
}

<style>
    MapVotePanel {
        position: absolute;
        width: 100%;
        height: 100%;
        justify-content: center;
        align-items: center;
        pointer-events: all;
        z-index: 500;

        .maps {
            display: flex;
            gap: 20px 20px;
            max-height: 750px;
            width: 1400px;
            flex-wrap: wrap;
            overflow: scroll;
            justify-content: center;
        }
    }
</style>